# Core data + training deps for DSA_OccluNet.
# Verified on Python 3.14 (CachyOS) with an RTX 5090.
numpy
pandas
pydicom
openpyxl
matplotlib
scikit-learn
joblib

# PyTorch: install the CUDA build that matches your GPU BEFORE the line below,
# because the default PyPI wheel is CPU-only. For the RTX 5090 (Blackwell / sm_120)
# use the cu128 build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (CPU-only is fine for --dry-run and scripts/smoke_test.py.)
torch

# Only needed to instantiate the real V-JEPA 2 backbone (not for --dry-run / smoke test).
transformers

# torchvision is only needed for the RadImageNet comparison (radimagenet_probe.py).
# Install the CUDA build matching your torch, e.g.:
#   pip install torchvision --index-url https://download.pytorch.org/whl/cu128
torchvision

# Hand-crafted radiomics features (radiomics_probe.py) for the classical-ML ensemble
# (ensemble_experiments.py). PyRadiomics is imported lazily, so it is NOT required for the
# neural probes, the ensemble on precomputed caches, or --synthetic-demo. Its published
# wheel does not build against NumPy >= 2 / recent Python, so install it in an ISOLATED
# environment and point radiomics_probe.py at that interpreter:
#   python3 -m venv .venv-radiomics
#   .venv-radiomics/bin/pip install "numpy<2" SimpleITK pyradiomics
# pyradiomics
# SimpleITK
